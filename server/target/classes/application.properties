# Server Configuration
server.port=8080
server.servlet.context-path=/

# MongoDB Configuration
# Use Atlas URI from your .env or environment variable
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://pushkarkumarsaini2006_db_user:fAhspdv4ICuhnjKr@leafstacklibrary.mdas4sd.mongodb.net/leafstack-library?retryWrites=true&w=majority&appName=LeafStackLibrary}
spring.data.mongodb.auto-index-creation=true

# JWT Configuration
jwt.secret=leafstack-secret-key-2025
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:8080
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Logging
logging.level.com.leafstack.library=DEBUG
logging.level.org.springframework.security=DEBUG

# Admin seeding (for initial login). Change these in production.
admin.seed.enabled=true
admin.seed.email=admin@leafstack.local
admin.seed.username=admin
admin.seed.password=admin12345